/* =========================================================
   Admin config — the DEPLOYED admin password.

   You normally don't edit this by hand. Instead:
     1. Open admin.html, unlock, click the ⚙️ Settings icon
     2. Change the password there (applies instantly on that browser)
     3. Click "Download admin-config.js" and upload the downloaded
        file over this one on your hosting.
   That makes the new password the default for every device.

   Note: this is a frontend-only site, so anyone who views the page
   source can read this password. It keeps casual visitors out — it is
   not real security. Don't reuse a password you use anywhere else.
   ========================================================= */

window.ADMIN_CONFIG = {
  password: "spark2026",
  ownerName: "Admin",
};
